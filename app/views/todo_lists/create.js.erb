$("#decorated_todo_lists").replaceWith("<%= escape_javascript(
  render :partial => "base_todo_lists", :locals => {:base_todo_lists => @user.base_todo_lists}
 ) %>");

 $("#import_list_"+<%=@todo_list.basecamp_id%>).remove();
 $("#"+<%=@todo_list.basecamp_id%>).children().children().remove();
